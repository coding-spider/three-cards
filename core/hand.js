'use strict';

class Hand {
    constructor() {
        this.cards = [];
    }

    addCard(card) {
        this.cards.push(card);
    }

    score() {
        // Score Calculator Logic
        let score = 0;
        return score;
    }
}

module.exports = Hand;